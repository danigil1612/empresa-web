{
  "name": "empresa-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web corporativa de Pulse amb accés extern per a clients",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "generate": "npm --prefix apps/web run generate",
    "preview": "npm --prefix apps/web run preview",
    "format": "echo \"Configurar Prettier més endavant\"",
    "lint": "echo \"Configurar ESLint més endavant\""
  },
  "workspaces": [
    "apps/web"
  ]
}
